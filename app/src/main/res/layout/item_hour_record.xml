<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp"

    >

    <TextView
        android:id="@+id/hour_record_item_time"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:ems="4"

        android:gravity="center_vertical"
        android:hint="06~07"

        app:layout_constraintBottom_toBottomOf="@+id/hour_record_item_text"


        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/hour_record_item_text"


        />

    <TextView
        android:id="@+id/hour_record_item_text"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:ems="14"


        android:gravity="center_vertical"
        android:textAlignment="center"


        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.6"
        app:layout_constraintHorizontal_chainStyle="packed"

        app:layout_constraintStart_toEndOf="@+id/hour_record_item_time"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>